apiVersion: v1
kind: Service
metadata:
  name: about
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "about"
    port: 8080
  selector:
    app: about
