apiVersion: v1
kind: Service
metadata:
  name: think
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "think"
    port: 8080
  selector:
    app: think
